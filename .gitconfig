[alias]
this-user = !git config --global --get user.name && git config --global --get user.email
init-ignores = !git update-index --assume-unchanged ./pom.xml && git update-index --assume-unchanged ./.idea/*.xml